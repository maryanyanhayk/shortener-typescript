{
  "name": "shortener-typescript",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace backend/shortener",
    "start": "npm start --workspace backend/shortener"
  },
  "workspaces": [
    "backend/shortener"
  ],
  "dependencies": {
    "axios": "^1.7.7",
    "path-to-regexp": "^8.2.0"
  }
}
